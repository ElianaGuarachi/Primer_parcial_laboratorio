
#include "EstadoPedidos.h"

int InicializarEstados(eEstado estadoPedidos[], int cantidad)
{
	int retorno=-1;
	if(estadoPedidos != NULL && cantidad > 0)
	{
		for(int i=0; i<cantidad; i++)
		{
			estadoPedidos[i].isEmpty = LIBRE;
			retorno=1;
		}
	}
	return retorno;
}

